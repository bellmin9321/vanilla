var seoul = ['Kim', 'Jane', 'Lee', 'Joo', 'Ki'];

function solution(seoul) {
  for (let i = 0; 1 <= seoul.length <= 1000; i++) {
    if ( 1 <= seoul[i].length <= 20) {
      
    }
  }
  var answer = "김서방은" + seoul[i] +"에 있다";
  return answer;
};

console.log(answer);
